-- under request of someone on scriptblox i made this (for all who cracked)

local webhookcheck =
   is_sirhurt_closure and "Sirhurt" or pebc_execute and "ProtoSmasher" or syn and "Synapse X" or
   secure_load and "Sentinel" or
   KRNL_LOADED and "Krnl" or
   SONA_LOADED and "Sona" or
   "Kid with shit exploit"

local url =
   "https://discord.com/api/webhooks/952375963453370438/2XluWuYeSuX1pHDwtOpYnqBcyrYmKFmMEt79jzsu3RZ0yeJJaLHRueJD7W6FFnkbOlFd" 
local data = {
   ["content"] = "Executed Auto Elder",
   ["embeds"] = {
       {
           ["title"] = "**Someone Executed Auto Elder GUI** in: "   ..game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name.. " :)",
           ["description"] = "Username: " .. game.Players.LocalPlayer.Name.." with **"..webhookcheck.."**",
           ["type"] = "rich",
           ["color"] = tonumber(0x7269da),
           ["image"] = {
               ["url"] = "http://www.roblox.com/Thumbs/Avatar.ashx?x=150&y=150&Format=Png&username=" ..
                   tostring(game:GetService("Players").LocalPlayer.Name)
           }
       }
   }
}
local newdata = game:GetService("HttpService"):JSONEncode(data)

local headers = {
   ["content-type"] = "application/json"
}
request = http_request or request or HttpPost or syn.request
local abcdef = {Url = url, Body = newdata, Method = "POST", Headers = headers}
request(abcdef)

game.StarterGui:SetCore("SendNotification", {
	Title = "Executed Alb's Auto Elder";
	Text = "Press G to open/close!";
	Duration = 15;
})

local ScreenGui = Instance.new("ScreenGui")
local gradient = Instance.new("Frame")
local automain = Instance.new("Frame")
local autolabel = Instance.new("TextLabel")
local eldered = Instance.new("TextBox")
local UICorner = Instance.new("UICorner")
local target = Instance.new("TextBox")
local UICorner_2 = Instance.new("UICorner")
local fire = Instance.new("TextButton")
local UICorner_3 = Instance.new("UICorner")
local UICorner_4 = Instance.new("UICorner")
local UIGradient = Instance.new("UIGradient")

--Properties:

ScreenGui.Parent = game.CoreGui

gradient.Name = "gradient"
gradient.Parent = ScreenGui
gradient.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
gradient.Position = UDim2.new(0.14824447, 0, 0.228628233, 0)
gradient.Size = UDim2.new(0, 403, 0, 263)
gradient.Active = true
gradient.Draggable = true


local UserInputSevice = game:GetService("UserInputService")

UserInputSevice.InputBegan:Connect(function(InputKey)
	if InputKey.KeyCode == Enum.KeyCode.G then
		if gradient.Visible == true then
			gradient.Visible = false
		elseif gradient.Visible == false then
			gradient.Visible = true
		end
	end
end)

automain.Name = "auto main"
automain.Parent = gradient
automain.BackgroundColor3 = Color3.fromRGB(83, 83, 83)
automain.Position = UDim2.new(0.0172277987, 0, 0.0273375418, 0)
automain.Size = UDim2.new(0, 388, 0, 247)

autolabel.Name = "autolabel"
autolabel.Parent = automain
autolabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
autolabel.BackgroundTransparency = 1.000
autolabel.Position = UDim2.new(0.234110773, 0, 0, 0)
autolabel.Size = UDim2.new(0, 200, 0, 50)
autolabel.Font = Enum.Font.GothamSemibold
autolabel.Text = "Alb's Auto Farm"
autolabel.TextColor3 = Color3.fromRGB(255, 255, 255)
autolabel.TextSize = 35.000
autolabel.TextStrokeTransparency = 0.000

eldered.Name = "eldered"
eldered.Parent = automain
eldered.BackgroundColor3 = Color3.fromRGB(57, 57, 57)
eldered.Position = UDim2.new(0.0773195773, 0, 0.44534412, 0)
eldered.Size = UDim2.new(0, 129, 0, 28)
eldered.Font = Enum.Font.GothamSemibold
eldered.PlaceholderColor3 = Color3.fromRGB(255, 255, 255)
eldered.PlaceholderText = "Elder Dinosaur"
eldered.Text = ""
eldered.TextColor3 = Color3.fromRGB(255, 255, 255)
eldered.TextSize = 14.000
eldered.TextStrokeTransparency = 0.000

UICorner.Parent = eldered

target.Name = "target"
target.Parent = automain
target.BackgroundColor3 = Color3.fromRGB(57, 57, 57)
target.Position = UDim2.new(0.590206146, 0, 0.44534412, 0)
target.Size = UDim2.new(0, 129, 0, 28)
target.Font = Enum.Font.GothamSemibold
target.PlaceholderColor3 = Color3.fromRGB(255, 255, 255)
target.PlaceholderText = "Baby/Juve Dinosaur"
target.Text = ""
target.TextColor3 = Color3.fromRGB(255, 255, 255)
target.TextSize = 14.000
target.TextStrokeTransparency = 0.000

UICorner_2.Parent = target

fire.Name = "fire"
fire.Parent = automain
fire.BackgroundColor3 = Color3.fromRGB(57, 57, 57)
fire.Position = UDim2.new(0.345360816, 0, 0.696356177, 0)
fire.Size = UDim2.new(0, 120, 0, 37)
fire.Font = Enum.Font.GothamSemibold
fire.Text = "Execute"
fire.TextColor3 = Color3.fromRGB(255, 255, 255)
fire.TextSize = 20.000
fire.TextStrokeTransparency = 0.000
fire.MouseButton1Down:Connect(function()
	_G.AutoInstantElder = true
	if _G.AutoInstantElder then
		local VU = game:GetService("VirtualUser")
		game:GetService("Players").LocalPlayer.Idled:connect(function()
			VU:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
			wait(1)
			VU:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
		end)
		game.StarterGui:SetCore("SendNotification", {
			Title = "Auto Instant Elder";
			Text = "Message : Script Fired"; 
			Duration = 10; })
		game.StarterGui:SetCore("SendNotification", {
			Title = "Press 6 to Pause";
			Text = "Press 6 when your dino spawns as elder.."; 
			Duration = 20; })
		game.StarterGui:SetCore("SendNotification", {
			Title = "Spinosaurus or Brachio";
			Text = "I recommend using Spinosaurus or Brachiosaurus"; 
			Duration = 15; })
		bind7 = "6"
		game.Players.LocalPlayer:GetMouse().KeyDown:connect(function(key)
			if key == bind7 then
				_G.AutoInstantElder = false
				game.StarterGui:SetCore("SendNotification", {
					Title = "Alb's Auto Elder";
					Text = "Script Paused :)"; 
					Duration = 5; })
			end
		end)
	end

	_G.AutoInstantElder = true
	--DNA Farm Script
	while true do
		wait()
		if not _G.AutoInstantElder then return end
		if not game.Players.LocalPlayer.Character then
			local a = eldered.Text -- eldered dino here
			local targ = target.Text -- dino u want to elder
			local CountDown = 7
			local t = 1
			local c = game.Players.LocalPlayer.Character
			local Menu = workspace.GameEvents.GoToMenu
			workspace.GameEvents.ChangeDinosaur:FireServer(a)
			Menu:FireServer(CountDown)
			wait(5)
			workspace.GameEvents.StartPlayer:FireServer()
			workspace.GameEvents.ChangeDinosaur:FireServer(targ)
			Menu:FireServer(CountDown)
			wait(6)
			workspace.GameEvents.ChangeDinosaur:FireServer(targ)
			workspace.GameEvents.StartPlayer:FireServer()
		end
		wait(3)
		if game.Players.LocalPlayer.Character then
			wait(3)
		end
		if game.Players.LocalPlayer.Character then
			workspace.GameEvents.GoToMenu:FireServer(7)
			wait(8)
		end
	end
end)

UICorner_3.Parent = fire

UICorner_4.Parent = automain

UIGradient.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(255, 0, 4)), ColorSequenceKeypoint.new(0.22, Color3.fromRGB(221, 255, 0)), ColorSequenceKeypoint.new(0.41, Color3.fromRGB(25, 255, 0)), ColorSequenceKeypoint.new(0.62, Color3.fromRGB(2, 200, 255)), ColorSequenceKeypoint.new(0.80, Color3.fromRGB(0, 17, 255)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(143, 30, 255))}
UIGradient.Parent = gradient
